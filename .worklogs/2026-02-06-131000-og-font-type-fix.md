# Fix OG route font type errors

## Changes
- `Weight` and `FontStyle` type aliases for Satori's expected literal unions
- Replaced `number` and `string` in font cache and FontEntry types

## Files Modified
- `landing/src/app/api/og/route.tsx`
